out vec4 output_color; // "output_color" replaces "gl_FragColor" because glBindFragDataLocation(...) has been called

in vec3 ps_position;
in vec3 ps_normal;

void main()
{
  output_color.rgb = normalize(ps_position) * 0.5 + 0.5;
  //output_color.rgb = vec3(1.0, 0.0, 0.0);
  output_color.a   = 1.0;
}