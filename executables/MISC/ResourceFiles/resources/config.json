{
	"windows": {
		"main_window": {
			"width": 1200,
			"height": 600
		}
	},

	"gpu": { "[include]": "$SCRIPT_PATH/gpu.json" },

	"game": { "[include]": "$SCRIPT_PATH/game.json" },

	"verify_caracter_escape": "$$escaped",

	"verify_path_concatenation": "$SCRIPT_PATH/one_path.txt"
}