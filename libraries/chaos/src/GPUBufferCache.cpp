#include <chaos/GPUBufferCache.h>

namespace chaos
{

    bool GPUBufferCache::GetBuffer(size_t required_size, GPURenderParams& render_params, shared_ptr<GPUBuffer>& result)
    {


        return false;
    }

}; // namespace chaos

