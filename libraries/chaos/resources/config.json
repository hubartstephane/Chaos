{
  "windows": {
    "main_window": {
      "default_size": [ 1200, 600 ]
    }
  },

  "max_tick_duration": 0.06,
  "forced_tick_durationxxx": 0.003,

  "gpu": { "[include]": "$SCRIPT_PATH/gpu.json" },
  "game": { "[include]": "$SCRIPT_PATH/game.json" },
  "sounds": { "[include]": "$SCRIPT_PATH/sounds.json" },
  "hud": { "[include]": "$SCRIPT_PATH/hud.json" },
  "classes": { "[include]": "$SCRIPT_PATH/classes.json" },

  "atlas": { "[include]": "$SCRIPT_PATH/atlas.json" },
  "imgui": { "[include]": "$SCRIPT_PATH/imgui.json" },
  "fonts": { "[include]": "fonts.json" },

  "sprite_directory": "$SCRIPT_PATH/sprites"
}